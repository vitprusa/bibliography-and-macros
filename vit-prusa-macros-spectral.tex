\newcommand{\Dmatrix}[1]{\tensor{\tensorc{D}}{_{#1}}} % OBSOLETE, for backward compatibility only, differentiation matrix components

\newcommand{\DMatrixc}[1]{\tensor{\tensorc{D}}{_{#1}}} % differentiation matrix components
\newcommand{\DMatrix}[1]{\tensorq{D}_{#1}} % differentiation matrix

\newcommand{\DDMatrixBC}[1]{\tensorq{D}_{#1, \, \mathrm{bc}}^2} % second order differentiation matrix, projection, boundary values

\newcommand{\DMatrixTilde}[1]{\widetilde{\tensorq{D}_{#1}}} % differentiation matrix, reduced 
\newcommand{\DDMatrixTilde}[1]{\widetilde{\tensorq{D}_{#1}^2}} % differentiation matrix, second order, reduced 

\newcommand{\GMatrixc}[1]{\tensor{\tensorc{G}}{_{#1}}} % Green matrix components
\newcommand{\GMatrix}[1]{\tensorq{G}_{#1}} % Green matrix

\newcommand{\GMatrixBC}[1]{\tensorq{G}_{#1, \, \mathrm{bc}}} % Green matrix, projection, dummy columns dealing with boundary values

\newcommand{\GFunction}{g} % Green function
\newcommand{\GOperator}{\mathcal G} % Green operator

\newcommand{\QOperator}{\mathcal Q} % coefficients operator

\newcommand{\IMatrix}[1]{\tensorq{I}_{#1}} % Integration matrix

\newcommand{\Pmatrix}[2]{\tensor{{\left( \tensorc{P}^{#1} \right)}}{_{#2}}} % resampling matrix

\newcommand{\bweight}{\lambda} % barycentric weight
\newcommand{\qweight}{w} % quadrature weight

\newcommand{\ChebyshevT}[1]{\mathrm{T}_{#1}} % Chebyshev polynomial
\newcommand{\cbar}[1]{\bar{c}_{#1}} % C bar coefficient in the discret orthogonality condition

%\newcommand{\vecelem}[1]{{\left\llbracket #1 \right\rrbracket}} % elements of vectors, numbering from one

\newcommand{\vecelem}[1]{#1} % elements of vectors, numbering from one

\newcommand{\opcompose}[2]{#1 \circ #2} % composition of functions/operators

\newcommand{\mkron}[2]{\tensortensor{#1}{#2}} % Kronecker product of matrices

\DeclareMathOperator{\DCTI}{DCT-I}
\DeclareMathOperator{\INTEGRATE}{INTEGRATE}
\DeclareMathOperator{\EXTEND}{EXTEND}
\DeclareMathOperator{\REDUCE}{REDUCE}
\DeclareMathOperator{\VEC}{VEC}
\DeclareMathOperator{\DVEC}{DE-VEC}
